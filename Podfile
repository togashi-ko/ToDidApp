# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ToDidApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for ToDidApp
    pod 'Firebase/Core'

  target 'ToDidAppTests' do
    inherit! :search_paths
    # Pods for testing]
    pod 'Firebase/Core'
  end

  target 'ToDidAppUITests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Firebase/Core'
  end

end
